---
import Layout from "@/layouts/Layout.astro";
import ResetPasswordForm from "@/components/auth/ResetPasswordForm";

// TODO: When implementing Supabase auth, check for valid session from password reset email
// If no valid session, redirect to forgot-password page
---

<Layout title="Reset Password - 10x Cards">
  <div class="min-h-[calc(100vh-64px)] flex items-center justify-center px-4 py-12 bg-background">
    <ResetPasswordForm client:only="react" />
  </div>
</Layout>
