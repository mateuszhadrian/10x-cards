name = "10x-cards"
compatibility_date = "2024-01-01"

# Public variables (non-sensitive)
# These are available in runtime through context.locals.runtime.env
[vars]
SUPABASE_URL = "https://ysbiohasdktsri.supabase.co"

# Secrets (set via Cloudflare Dashboard):
# SUPABASE_KEY - add as "Encrypted" secret in Dashboard
# OPENROUTER_API_KEY - add as "Encrypted" secret in Dashboard
# 
# Note: For local development, you can also use .dev.vars file with:
# SUPABASE_KEY=your_key
# OPENROUTER_API_KEY=your_key
