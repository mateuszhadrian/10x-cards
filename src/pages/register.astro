---
import Layout from "@/layouts/Layout.astro";
import RegisterForm from "@/components/auth/RegisterForm";

// Check if user is already logged in - redirect to flashcards
// TODO: Implement this check when auth middleware is ready
// const user = Astro.locals.user;
// if (user) {
//   return Astro.redirect('/flashcards');
// }
---

<Layout title="Create Account - 10x Cards">
  <div class="min-h-[calc(100vh-64px)] flex items-center justify-center px-4 py-12 bg-background">
    <RegisterForm client:only="react" />
  </div>
</Layout>
